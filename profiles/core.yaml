id: core
name: Core Developer
description: Container tooling for isolated dev environments - version control, a shell, and the other baseline tools are already part of every SWAT install.
version: 1

host_packages:
  - podman
  - distrobox

recommended_apps:
  - visual-studio-code

checks:
  - podman --version
  - distrobox --version
