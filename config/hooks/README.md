# Hooks

Scripts run at defined points during the `live-build` build process (chroot and binary hooks). Review any script here before running a build — they execute with root privileges inside the chroot.
