# includes.chroot_before_packages

Files placed here are copied directly into the chroot's filesystem before packages are installed (e.g. `etc/apt/preferences.d/` pinning, `etc/` overrides that package postinst scripts should see).
