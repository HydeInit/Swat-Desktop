# Core system packages beyond the base install
locales
sudo
console-setup
keyboard-configuration
plymouth
# Provides pkexec - the polkit authentication agent (already pulled in
# indirectly) needs this to actually exist for graphical privilege
# elevation (e.g. the SWAT installer launcher) to work.
policykit-1
