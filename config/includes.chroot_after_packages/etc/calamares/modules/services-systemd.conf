# Services to ensure are enabled on the target system. These are
# already enabled by systemd presets when their packages were
# installed during the build, but list them explicitly to avoid
# depending on that being true across package updates.
---
services:
    - name: NetworkManager
      mandatory: true
    - name: lightdm
      mandatory: true
