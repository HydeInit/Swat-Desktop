# Which display manager to configure on the target system. Calamares
# can't infer this - it must be told explicitly, or the module fails
# outright ("displaymanagers list is empty").
---
displaymanagers:
    - lightdm

basicSetup: false
