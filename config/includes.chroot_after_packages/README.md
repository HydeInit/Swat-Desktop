# includes.chroot_after_packages

Files placed here are copied directly into the chroot's filesystem after packages are installed (branding assets, default dotfiles, config overrides that packages would otherwise overwrite).
