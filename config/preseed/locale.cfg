# Pre-fills (doesn't skip) the installer's locale/timezone prompts with
# sensible defaults, matching the live session. User account creation is
# deliberately left unpreseeded - that's a real per-install choice, not
# something to default.
d-i debian-installer/locale string en_US.UTF-8
d-i time/zone string UTC
d-i clock-setup/utc boolean true
